import type { EnvAdapter } from "../core/adapters.js";
export type PrecisionTier = "address" | "locality" | "district" | "region" | "country"; export type ScoredPrecisionTier = PrecisionTier | "unknown"; export type DatePrecision = "day" | "month" | "year" | "decade" | "range" | "inferred";
export interface PresenceProjection { readonly subject: number; readonly spouse?: number; readonly mother?: number; readonly father?: number; readonly parents?: number }
export interface SamesoilConfig { readonly engine_version: string; readonly date: { readonly abt_years: number; readonly est_years: number; readonly cal_years: number; readonly open_interval_fallback_years: number; readonly pre_1752_widen_days: number }; readonly presence: { readonly point_event_dwell_days: number; readonly transit_event_window_days: number; readonly min_presence_conf: number; readonly projections: Readonly<Record<string, PresenceProjection>> }; readonly place: { readonly radius_km_by_tier: Readonly<Record<PrecisionTier, number>>; readonly min_tier: PrecisionTier; readonly proximity_radius_multiplier: number; readonly min_geocode_conf: number }; readonly kinship: { readonly close_kin_cutoff: number; readonly in_law_cutoff: number; readonly generations_cutoff: number; readonly branch_cutoff: number; readonly bfs_max_depth: number }; readonly scoring: { readonly weights: { readonly proximity: number; readonly temporal: number; readonly precision: number; readonly confidence: number; readonly unrelatedness: number; readonly independence: number }; readonly precision_tier_weights: Readonly<Record<ScoredPrecisionTier, number>>; readonly temporal_precision_weights: Readonly<Record<DatePrecision, number>>; readonly temporal_overlap_full_days: number; readonly temporal_min_overlap_factor: number; readonly temporal_candidate_window_days: number; readonly temporal_gap_full_score_days: number; readonly temporal_gap_min_factor: number; readonly independence_same_record: number; readonly independence_distinct_record: number; readonly score_threshold: number; readonly max_candidates: number }; readonly privacy: { readonly include_living: boolean; readonly living_cutoff_year: number } }
export interface LoadedConfig { readonly config: SamesoilConfig; readonly hash: string; readonly canonicalJson: string }
type JsonObject = Record<string, unknown>; const isObject = (value: unknown): value is JsonObject => typeof value === "object" && value !== null && !Array.isArray(value);
function merge(defaults: unknown, override: unknown, path: string): unknown { if (override === undefined) return defaults; if (isObject(defaults)) { if (!isObject(override)) throw new Error(`${path}: expected object`); const unknown = Object.keys(override).filter((key) => !(key in defaults)); if (unknown.length > 0) throw new Error(`${path}.${unknown[0]}: unknown key`); return Object.fromEntries(Object.keys(defaults).map((key) => [key, merge(defaults[key], override[key], `${path}.${key}`)])); } return override; }
function requireObject(value: unknown, path: string): JsonObject { if (!isObject(value)) throw new Error(`${path}: expected object`); return value; } function requireString(value: unknown, path: string): string { if (typeof value !== "string" || value.length === 0) throw new Error(`${path}: expected non-empty string`); return value; } function requireNumber(value: unknown, path: string): number { if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`${path}: expected finite number`); return value; } function requireBoolean(value: unknown, path: string): boolean { if (typeof value !== "boolean") throw new Error(`${path}: expected boolean`); return value; }
function numberObject(value: unknown, path: string, keys: readonly string[]): Record<string, number> { const object = requireObject(value, path); return Object.fromEntries(keys.map((key) => [key, requireNumber(object[key], `${path}.${key}`)])); }
function projections(value: unknown, path: string): Readonly<Record<string, PresenceProjection>> { const object = requireObject(value, path), result: Record<string, PresenceProjection> = {}; for (const key of Object.keys(object).sort()) { const row = requireObject(object[key], `${path}.${key}`), allowed = ["subject", "spouse", "mother", "father", "parents"], unknown = Object.keys(row).filter((name) => !allowed.includes(name)); if (unknown.length > 0) throw new Error(`${path}.${key}.${unknown[0]}: unknown key`); const projection: Record<string, number> = { subject: requireNumber(row.subject, `${path}.${key}.subject`) }; for (const name of allowed.slice(1)) if (row[name] !== undefined) projection[name] = requireNumber(row[name], `${path}.${key}.${name}`); result[key] = projection as unknown as PresenceProjection; } return result; }
function validate(value: unknown): SamesoilConfig { const root = requireObject(value, "config"), date = numberObject(root.date, "config.date", ["abt_years", "est_years", "cal_years", "open_interval_fallback_years", "pre_1752_widen_days"]), presenceRoot = requireObject(root.presence, "config.presence"), presenceNumbers = numberObject(presenceRoot, "config.presence", ["point_event_dwell_days", "transit_event_window_days", "min_presence_conf"]), place = requireObject(root.place, "config.place"), radii = numberObject(place.radius_km_by_tier, "config.place.radius_km_by_tier", ["address", "locality", "district", "region", "country"]), kinship = numberObject(root.kinship, "config.kinship", ["close_kin_cutoff", "in_law_cutoff", "generations_cutoff", "branch_cutoff", "bfs_max_depth"]), scoring = requireObject(root.scoring, "config.scoring"), weights = numberObject(scoring.weights, "config.scoring.weights", ["proximity", "temporal", "precision", "confidence", "unrelatedness", "independence"]), precisionWeights = numberObject(scoring.precision_tier_weights, "config.scoring.precision_tier_weights", ["address", "locality", "district", "region", "country", "unknown"]), temporalWeights = numberObject(scoring.temporal_precision_weights, "config.scoring.temporal_precision_weights", ["day", "month", "year", "decade", "range", "inferred"]), privacy = requireObject(root.privacy, "config.privacy"), tier = requireString(place.min_tier, "config.place.min_tier"); if (!["address", "locality", "district", "region", "country"].includes(tier)) throw new Error("config.place.min_tier: invalid precision tier"); return { engine_version: requireString(root.engine_version, "config.engine_version"), date: date as unknown as SamesoilConfig["date"], presence: { ...presenceNumbers, projections: projections(presenceRoot.projections, "config.presence.projections") } as unknown as SamesoilConfig["presence"], place: { radius_km_by_tier: radii as unknown as Readonly<Record<PrecisionTier, number>>, min_tier: tier as PrecisionTier, proximity_radius_multiplier: requireNumber(place.proximity_radius_multiplier, "config.place.proximity_radius_multiplier"), min_geocode_conf: requireNumber(place.min_geocode_conf, "config.place.min_geocode_conf") }, kinship: kinship as unknown as SamesoilConfig["kinship"], scoring: { weights: weights as unknown as SamesoilConfig["scoring"]["weights"], precision_tier_weights: precisionWeights as unknown as SamesoilConfig["scoring"]["precision_tier_weights"], temporal_precision_weights: temporalWeights as unknown as SamesoilConfig["scoring"]["temporal_precision_weights"], temporal_overlap_full_days: requireNumber(scoring.temporal_overlap_full_days, "config.scoring.temporal_overlap_full_days"), temporal_min_overlap_factor: requireNumber(scoring.temporal_min_overlap_factor, "config.scoring.temporal_min_overlap_factor"), temporal_candidate_window_days: requireNumber(scoring.temporal_candidate_window_days, "config.scoring.temporal_candidate_window_days"), temporal_gap_full_score_days: requireNumber(scoring.temporal_gap_full_score_days, "config.scoring.temporal_gap_full_score_days"), temporal_gap_min_factor: requireNumber(scoring.temporal_gap_min_factor, "config.scoring.temporal_gap_min_factor"), independence_same_record: requireNumber(scoring.independence_same_record, "config.scoring.independence_same_record"), independence_distinct_record: requireNumber(scoring.independence_distinct_record, "config.scoring.independence_distinct_record"), score_threshold: requireNumber(scoring.score_threshold, "config.scoring.score_threshold"), max_candidates: requireNumber(scoring.max_candidates, "config.scoring.max_candidates") }, privacy: { include_living: requireBoolean(privacy.include_living, "config.privacy.include_living"), living_cutoff_year: requireNumber(privacy.living_cutoff_year, "config.privacy.living_cutoff_year") } }; }
function canonicalize(value: unknown): string { if (Array.isArray(value)) return `[${value.map(canonicalize).join(",")}]`; if (isObject(value)) return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalize(value[key])}`).join(",")}}`; return JSON.stringify(value); }
export async function loadConfig(defaultJson: string, overrideJson: string | undefined, env: EnvAdapter): Promise<LoadedConfig> { let defaults: unknown, override: unknown = undefined; try { defaults = JSON.parse(defaultJson); } catch { throw new Error("config defaults: invalid JSON"); } if (overrideJson !== undefined) { try { override = JSON.parse(overrideJson); } catch { throw new Error("config override: invalid JSON"); } } const config = validate(merge(defaults, override, "config")), canonicalJson = canonicalize(config); return { config, canonicalJson, hash: await env.sha256(canonicalJson) }; }
