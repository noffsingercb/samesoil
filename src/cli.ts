import { resolve } from "node:path";
import { NodeStorageAdapter } from "./adapters/node/storage.js"; import { NodeArtifactSinkAdapter, NodeFileSourceAdapter } from "./adapters/node/files.js"; import { ConsoleProgressReporter } from "./adapters/node/progress.js"; import { NodeEnvAdapter } from "./adapters/node/env.js"; import { NominatimPlaceProvider } from "./adapters/node/nominatim-place-provider.js"; import { loadConfig } from "./config/config.js"; import type { EngineContext } from "./core/context.js"; import { migrate } from "./db/migrate.js"; import { run as ingest } from "./passes/ingest.js"; import { run as dates } from "./passes/dates.js"; import { run as places } from "./passes/places.js"; import { run as kinship } from "./passes/kinship.js"; import { run as presence } from "./passes/presence.js"; import { run as candidates } from "./passes/candidates.js"; import { run as score } from "./passes/score.js"; import { run as exportPass } from "./passes/export.js";
type Command = "init" | "ingest" | "dates" | "places" | "kinship" | "presence" | "candidates" | "score" | "export" | "all"; const COMMANDS: readonly Command[] = ["init", "ingest", "dates", "places", "kinship", "presence", "candidates", "score", "export", "all"];
interface Options { readonly command: Command; readonly dbPath: string; readonly configPath?: string; readonly gedcomPath?: string; readonly verbose: boolean; readonly onlineGeocode: boolean; readonly geocodeDelayMs?: number; readonly geocodeEndpoint?: string; readonly geocodeUserAgent?: string }
function numberFlag(raw: string, flag: string): number { const value = Number(raw); if (!Number.isFinite(value) || value < 0) throw new Error(`${flag} requires a non-negative number`); return value; }
function parseArgs(argv: readonly string[]): Options { const command = argv[0] as Command; if (!COMMANDS.includes(command)) throw new Error(`Usage: samesoil <${COMMANDS.join("|")}> [--db path] [--config path] [--gedcom path] [--verbose] [--online-geocode --geocode-delay-ms number]`); let dbPath = "samesoil.sqlite", configPath: string | undefined, gedcomPath: string | undefined, verbose = false, onlineGeocode = false, geocodeDelayMs: number | undefined, geocodeEndpoint: string | undefined, geocodeUserAgent: string | undefined; for (let index = 1; index < argv.length; index += 1) { const arg = argv[index]; if (arg === "--verbose") verbose = true; else if (arg === "--online-geocode") onlineGeocode = true; else if (arg === "--db" || arg === "--config" || arg === "--gedcom" || arg === "--geocode-delay-ms" || arg === "--geocode-endpoint" || arg === "--geocode-user-agent") { const value = argv[index + 1]; if (value === undefined) throw new Error(`${arg} requires a value`); if (arg === "--db") dbPath = value; else if (arg === "--config") configPath = value; else if (arg === "--gedcom") gedcomPath = value; else if (arg === "--geocode-delay-ms") geocodeDelayMs = numberFlag(value, arg); else if (arg === "--geocode-endpoint") geocodeEndpoint = value; else geocodeUserAgent = value; index += 1; } else throw new Error(`Unknown argument: ${arg}`); } if (onlineGeocode && geocodeDelayMs === undefined) throw new Error("--online-geocode requires an explicit --geocode-delay-ms value"); if (onlineGeocode && command !== "places" && command !== "all") throw new Error("--online-geocode is only valid with places or all"); return { command, dbPath: resolve(dbPath), verbose, onlineGeocode, ...(configPath === undefined ? {} : { configPath: resolve(configPath) }), ...(gedcomPath === undefined ? {} : { gedcomPath: resolve(gedcomPath) }), ...(geocodeDelayMs === undefined ? {} : { geocodeDelayMs }), ...(geocodeEndpoint === undefined ? {} : { geocodeEndpoint }), ...(geocodeUserAgent === undefined ? {} : { geocodeUserAgent }) }; }
export async function main(argv: readonly string[] = process.argv.slice(2)): Promise<void> { const options = parseArgs(argv), files = new NodeFileSourceAdapter(), env = new NodeEnvAdapter(), root = process.cwd(), defaults = await files.readText(resolve(root, "config/default.json")), override = options.configPath === undefined ? undefined : await files.readText(options.configPath), loaded = await loadConfig(defaults, override, env), storage = new NodeStorageAdapter(options.dbPath); try { const schemaSql = await files.readText(resolve(root, "src/db/schema.sql")); migrate(storage, schemaSql); const placeProvider = options.onlineGeocode ? new NominatimPlaceProvider(options.geocodeEndpoint, options.geocodeUserAgent, options.geocodeDelayMs) : undefined, ctx: EngineContext = { storage, fileSource: files, artifactSink: new NodeArtifactSinkAdapter(resolve(options.dbPath, "../artifacts")), progress: new ConsoleProgressReporter(options.verbose), env, config: loaded.config, configHash: loaded.hash, ...(options.gedcomPath === undefined ? {} : { inputPath: options.gedcomPath }), ...(placeProvider === undefined ? {} : { placeProvider }) }; if (options.command === "ingest") await ingest(ctx); else if (options.command === "dates") await dates(ctx); else if (options.command === "places") await places(ctx); else if (options.command === "kinship") await kinship(ctx); else if (options.command === "presence") await presence(ctx); else if (options.command === "candidates") await candidates(ctx); else if (options.command === "score") await score(ctx); else if (options.command === "export") await exportPass(ctx); else if (options.command === "all") { await ingest(ctx); await dates(ctx); await places(ctx); await kinship(ctx); await presence(ctx); await candidates(ctx); await score(ctx); await exportPass(ctx); } console.log(`command=${options.command}`); console.log(`db=${options.dbPath}`); console.log(`config_hash=${loaded.hash}`); } finally { storage.close(); } }
main().catch((error: unknown): void => { console.error(error instanceof Error ? error.message : String(error)); process.exitCode = 1; });
